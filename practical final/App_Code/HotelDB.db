CREATE TABLE Users (
    Username TEXT PRIMARY KEY,
    Password TEXT NOT NULL,
    Role TEXT NOT NULL   -- "client" / "receptionist"
);

CREATE TABLE Clients (
    ClientID INTEGER PRIMARY KEY AUTOINCREMENT,
    Name TEXT,
    DOB TEXT,
    Address TEXT,
    Mobile TEXT
);

CREATE TABLE Reservations (
    ReservationID INTEGER PRIMARY KEY AUTOINCREMENT,
    ClientID INTEGER,
    Arrival TEXT,
    Departure TEXT,
    RoomType TEXT,
    FOREIGN KEY(ClientID) REFERENCES Clients(ClientID)
);

-- 测试账户
INSERT INTO Users VALUES ('client1', '123', 'client');
INSERT INTO Users VALUES ('reception1', '123', 'receptionist');
